all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

hayden:
  vars:
    ansible_python_interpreter: /usr/bin/python3
  hosts:
    device-server-9:
      ansible_user: hlittle
      ansible_host: device-server-9
    vm:
      ansible_user: hlittle
      ansible_host: vm
    small-yocto-builder:
      ansible_user: hlittle
      ansible_host: small-yocto-builder
